#include<stdio.h>
#include<conio.h>
int main(){
    int hyp, perpendicular, base,result;
    perpendicular = 4;
    base = 5;
    hyp = perpendicular*perpendicular+base*base;
    result = int sqrt(hyp);
    printf("%f",hyp);
    printf("hello world");
}